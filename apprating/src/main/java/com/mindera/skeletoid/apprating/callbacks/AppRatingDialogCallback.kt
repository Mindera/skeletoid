package com.mindera.skeletoid.apprating.callbacks

/**
 * Callback to prompt the rating dialog. To use if intended to create a custom dialog
 */
interface AppRatingDialogCallback {
    fun showRatingDialog()
}
