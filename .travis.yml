language: android
jdk: oraclejdk8

android:
  components:
    # The BuildTools version used by your project
    - tools
    - build-tools-25.0.2

    # The SDK version used to compile your project
    - android-25
  licenses:
    - '.+'
    
script:
  # Compile the SDK
  - ./gradlew assemble

  # Run initTests
  - ./gradlew test
